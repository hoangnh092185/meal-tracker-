import { Component Input, Output, EventEmitter } from '@angular/core';
import { Meal } from './meal.model';

@Component({
    Selector: 'total-calories',
    template: `
    <h2>Average daily calories
    `
})
